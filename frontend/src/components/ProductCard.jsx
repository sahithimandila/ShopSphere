import { Link } from 'react-router-dom'; import { useStore } from '../context/StoreContext';
export default function ProductCard({ product }) { const { addToCart, wishlist, toggleWishlist } = useStore(); const saved = wishlist.some(x => x.id === product.id); return <article className="product-card"><div className="product-image"><Link to={`/products/${product.id}`}><img src={product.image} alt={product.name}/></Link><span className="badge-soft">{product.badge}</span><button className={`wish ${saved?'active':''}`} onClick={()=>toggleWishlist(product)}><i className={`bi bi-heart${saved?'-fill':''}`}/></button><button className="quick-add" onClick={()=>addToCart(product)}>Add to bag</button></div><Link to={`/products/${product.id}`} className="product-info"><p>{product.brand}</p><h3>{product.name}</h3><div><strong>₹{product.price.toLocaleString('en-IN')}</strong><del>₹{product.oldPrice.toLocaleString('en-IN')}</del><span className="discount">{Math.round((1-product.price/product.oldPrice)*100)}% off</span></div><small><i className="bi bi-star-fill"/> {product.rating} <span>({product.reviews})</span></small></Link></article> }
