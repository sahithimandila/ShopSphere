import { Link, useNavigate } from 'react-router-dom'; import { useState } from 'react'; import { useStore } from '../context/StoreContext';
export default function Login(){const {setUser}=useStore();const nav=useNavigate();const [email,setEmail]=useState('');const submit=e=>{e.preventDefault();setUser({name:email.split('@')[0]||'Shopper',email});nav('/')};return <div className="auth-page"><form onSubmit={submit} className="auth-card"><Link className="brand" to="/">shop<span>sphere</span></Link><span className="eyebrow">Welcome back</span><h1>Sign in to your world.</h1><label>Email address<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength="6" type="password" placeholder="••••••••"/></label><button className="btn-dark w-100">Sign in</button><p>New here? <Link to="/register">Create an account</Link></p></form></div>}
