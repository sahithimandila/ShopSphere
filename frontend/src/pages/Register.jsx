import { Link, useNavigate } from 'react-router-dom'; import { useState } from 'react'; import { useStore } from '../context/StoreContext';
export default function Register(){const {setUser}=useStore();const nav=useNavigate();const [data,setData]=useState({name:'',email:''});const submit=e=>{e.preventDefault();setUser(data);nav('/')};return <div className="auth-page"><form onSubmit={submit} className="auth-card"><Link className="brand" to="/">shop<span>sphere</span></Link><span className="eyebrow">Join ShopSphere</span><h1>Good things await.</h1><label>Full name<input required value={data.name} onChange={e=>setData({...data,name:e.target.value})} placeholder="Your name"/></label><label>Email address<input required type="email" value={data.email} onChange={e=>setData({...data,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input required minLength="6" type="password" placeholder="At least 6 characters"/></label><button className="btn-dark w-100">Create account</button><p>Already a member? <Link to="/login">Sign in</Link></p></form></div>}
