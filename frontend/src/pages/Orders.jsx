import { Link, useLocation } from 'react-router-dom'; import { useStore } from '../context/StoreContext';
export default function Orders(){const {state}=useLocation();const {user}=useStore();return <div className="container page"><span className="eyebrow">Your purchases</span><h1>Orders</h1>{state?.placed&&<div className="success-banner"><i className="bi bi-check2-circle"/><div><b>Your order is confirmed.</b><br/>We’ll send tracking details to your email shortly.</div></div>}{user?<div className="empty compact"><i className="bi bi-box-seam"/><h2>No previous orders</h2><p>Your order history will live here.</p><Link className="btn-dark" to="/products">Keep shopping</Link></div>:<div className="empty compact"><i className="bi bi-person-lock"/><h2>Sign in to see your orders</h2><Link className="btn-dark" to="/login">Sign in</Link></div>}</div>}
