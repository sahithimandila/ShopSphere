import { Link } from 'react-router-dom'; import { useStore } from '../context/StoreContext'; import ProductCard from '../components/ProductCard';
export default function Wishlist(){const {wishlist}=useStore();return <div className="container page"><span className="eyebrow">Saved for later</span><h1>Your wishlist</h1>{wishlist.length?<div className="product-grid">{wishlist.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty"><i className="bi bi-heart"/><h2>A little empty here</h2><p>Save your favourites and they will appear here.</p><Link className="btn-dark" to="/products">Discover products</Link></div>}</div>}
